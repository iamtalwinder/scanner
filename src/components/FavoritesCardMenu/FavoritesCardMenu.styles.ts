import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  item: {
    marginTop: 50,
    position: 'absolute',
    width: '40%',
    right: 0,
    marginBottom: 0,
    paddingBottom: 0,
    backgroundColor: '#333333',
  },
  menuItem: {
    backgroundColor: '#333333'
  }
});
